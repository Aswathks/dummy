Step 1 — FastAPI Upload + OCR Implementation
Right now your goal is only this:
Plain text
Upload image → OCR text extraction → save text file → return OCR output
Do NOT do SQLite, RAG, chatbot, dashboard now.
Only Step 1.
What you need to do now
1. Install required packages
Open terminal inside project folder:
Bash
pip install fastapi uvicorn python-multipart pillow pytesseract
2. Install Tesseract OCR (Very Important)
Download and install:
Plain text
Tesseract OCR for Windows
After install, verify path:
Plain text
C:\Program Files\Tesseract-OCR\tesseract.exe
This path is important.
3. Create file
Plain text
backend/ocr_service.py
Code:
Python
from PIL import Image
import pytesseract

# Windows Tesseract path
pytesseract.pytesseract.tesseract_cmd = r"C:\Program Files\Tesseract-OCR\tesseract.exe"


def extract_text_from_image(image_path):
    """
    Extract text from uploaded image using OCR
    """

    image = Image.open(image_path)

    extracted_text = pytesseract.image_to_string(image)

    return extracted_text
4. Update backend/app.py
Use this clean version:
Python
from fastapi import FastAPI, UploadFile, File
from fastapi.middleware.cors import CORSMiddleware
from pathlib import Path
import shutil

from ocr_service import extract_text_from_image

app = FastAPI()

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

BASE_DIR = Path(__file__).resolve().parent.parent

RECEIPT_FOLDER = BASE_DIR / "backend" / "temp_uploads" / "receipts"
BANK_FOLDER = BASE_DIR / "backend" / "temp_uploads" / "bank_statements"

TEXT_RECEIPT_FOLDER = BASE_DIR / "exports" / "extracted_text" / "receipts"
TEXT_BANK_FOLDER = BASE_DIR / "exports" / "extracted_text" / "bank_statements"

RECEIPT_FOLDER.mkdir(parents=True, exist_ok=True)
BANK_FOLDER.mkdir(parents=True, exist_ok=True)
TEXT_RECEIPT_FOLDER.mkdir(parents=True, exist_ok=True)
TEXT_BANK_FOLDER.mkdir(parents=True, exist_ok=True)


@app.get("/")
def home():
    return {"message": "Personal Expense Tracker API Running"}


@app.post("/upload-file")
def upload_file(
    file: UploadFile = File(...),
    file_type: str = "receipt"
):
    # Decide save folders
    if file_type == "bank_statement":
        save_folder = BANK_FOLDER
        text_folder = TEXT_BANK_FOLDER
    else:
        save_folder = RECEIPT_FOLDER
        text_folder = TEXT_RECEIPT_FOLDER

    # Save uploaded image
    save_path = save_folder / file.filename

    with open(save_path, "wb") as buffer:
        shutil.copyfileobj(file.file, buffer)

    # OCR extraction
    extracted_text = extract_text_from_image(save_path)

    # Save OCR text file
    text_file_path = text_folder / f"{Path(file.filename).stem}.txt"

    with open(text_file_path, "w", encoding="utf-8") as f:
        f.write(extracted_text)

    return {
        "status": "success",
        "file_name": file.filename,
        "file_type": file_type,
        "saved_image_path": str(save_path),
        "saved_text_path": str(text_file_path),
        "ocr_text": extracted_text
    }
5. Run FastAPI
Inside terminal:
Bash
cd backend
uvicorn app:app --reload
You should see:
Plain text
Uvicorn running on http://127.0.0.1:8000
6. Open Swagger UI
Open browser:
Plain text
http://127.0.0.1:8000/docs
You will see:
Plain text
POST /upload-file
Click it.
7. Upload image
Use one of your receipt images first.
Example:
Plain text
apple_receipt.jpg
Choose:
Plain text
file_type = receipt
Then click:
Plain text
Execute
8. Expected output
You should get response like:
JSON
{
  "status": "success",
  "file_name": "apple_receipt.jpg",
  "file_type": "receipt",
  "saved_image_path": "...",
  "saved_text_path": "...",
  "ocr_text": "Apple Store Receipt..."
}
After Step 1 success
Checklist:
Plain text
✅ Image upload works
✅ OCR text extracted
✅ OCR text saved as .txt

NEXT:
⬜ Parser
⬜ SQLite
⬜ Categorization
⬜ Dashboard
⬜ Embeddings
⬜ RAG chatbot
⬜ n8n automation
Your task now
Complete only this first.
Do not move to Step 2 until OCR works perfectly.